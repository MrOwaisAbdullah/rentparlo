-- =============================================
-- 13. AUTHENTICATION ENHANCEMENTS DOCUMENTATION
-- =============================================

-- Summary of Authentication Enhancements
/*
The schema now includes comprehensive authentication enhancements to ensure data integrity 
and provide clear error messages for user registration:

1. UNIQUE CONSTRAINTS:
   - User email addresses (excluding NULLs)
   - User phone numbers (excluding NULLs)  
   - User guest_ids (excluding NULLs)
   - Seller phone numbers (excluding NULLs)
   - Seller email addresses (excluding NULLs)

2. FORMAT VALIDATION:
   - Pakistani phone numbers: ^03[0-9]{2}[0-9]{7}$
   - Email addresses: Standard email format validation
   - Pakistani CNIC numbers: ^[0-9]{5}-[0-9]{7}-[0-9]{1}$

3. HELPER FUNCTIONS:
   - check_user_uniqueness(): Validates email and phone uniqueness
   - check_seller_uniqueness(): Validates username and CNIC uniqueness
   - register_user_with_validation(): Enhanced user registration with validation
   - register_seller_with_validation(): Enhanced seller registration with validation

4. ERROR HANDLING:
   - Clear, user-friendly error messages for all validation scenarios
   - Proper error categorization and handling
   - Comprehensive exception handling

These enhancements ensure that all critical user information remains unique across the platform 
while providing a better user experience during registration with clear guidance and error messages.
*/

-- =============================================
-- END OF SCHEMA
-- =============================================